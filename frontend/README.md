# Note keeping app (Frontend) V.1.0

A simple note keeping app using React (vite + JS) & Tailwindcss. 

You should be able to create notes in different colors, write down your thoughts on the notes, move them around, and delete them.

Note position and text should be saved using LocalStorage. 
